'use strict';

// const useKarma = require('..');
import useKarma from '../lib/useKarma.js'

describe('useKarma', () => {
    it('needs tests', () => {
        expect(useKarma.add(1, 2), 3)
    });
    it('needs tests', () => {
        expect(useKarma.add(1, 2), 4)
    });
});
